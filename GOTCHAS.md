# Gotchas Log

Living log of pitfalls, edge cases, and workflow surprises discovered while working in this repo. Add a new entry whenever you hit or learn something worth remembering.

## Format
- `YYYY-MM-DD | Area | Short title` - One sentence describing the gotcha and how to avoid it.

## Log
- `YYYY-MM-DD | Area | Short title` - Description.
- `2026-01-13 | Swift | Task name collision` - Use `Swift.Task {}` to avoid confusion with the `Task` model; `_Concurrency.Task` can produce misleading DocumentSnapshot errors.
- `2026-01-13 | Web | Missing canvas-confetti dependency` - `npm run build` fails unless `canvas-confetti` is installed or externalized in Vite.
- `2026-01-13 | iOS Build | DerivedData nanodp build path` - `xcodebuild` can fail if a file exists at `DerivedData/.../nanopb/build`; remove the file so the directory can be created.
- `2026-01-13 | SwiftUI | onChange needs Equatable inputs` - Use `.map(\.id)` or another Equatable projection when observing arrays in `onChange`.
- `2026-01-13 | SwiftUI | Type-check timeouts on large views` - Break large SwiftUI bodies into smaller subviews or use `AnyView` to reduce compiler load.
- `2026-01-13 | Xcode | DerivedData nanonpb build path collision` - If the default `xcodebuild` target command fails with \"File exists but is not a directory\" under DerivedData, build with an explicit `-derivedDataPath` and `-scheme` instead.
- `2026-01-13 | Functions | AI model consistency` - Force text generation to use `gemini-3-flash-preview` in Cloud Functions to avoid client-provided model drift.
- `2026-01-13 | Functions | Deployed model mismatch` - If Cloud Functions aren’t deployed after model updates, production can still call deprecated models like `gemini-1.5-pro` and return 404s.
- `2026-01-13 | Functions | Deploy lockfile sync` - `firebase deploy` runs `npm ci`, so `functions/package-lock.json` must be in sync with `functions/package.json` or deployment fails.
- `2026-03-04 | Web Finance | Break-even edge case` - If contribution per unit is `<= 0`, there is no finite break-even point; always display an explicit \"not reachable\" state instead of a numeric threshold.
- `2026-03-04 | Web i18n | Mojibake in German locale files` - Some `web/locales/*-de.ts` entries can contain misencoded UTF-8 sequences (for example `Ã¼`, `â€“`); normalize to real Unicode characters and run a grep check before shipping.
- `2026-03-04 | Integrations | ProjectFlow token date shape` - Callable responses can return Firestore timestamps as objects (`seconds`/`_seconds`) instead of JS Dates, so settings UI should parse both shapes before formatting.
- `2026-03-05 | Quick Access | Compact clipboard can disappear with stale selection` - If `selectedItemId` points to a removed pin, compact-mode controls may vanish; always resolve an active fallback item (focus or first pinned) before rendering compact/full actions.
- `2026-03-05 | Finance Integrations | Endpoint tokens must stay in tenant secrets` - Store external financial tokens under `tenants/{tenantId}/secrets/*` and fetch telemetry through callable proxies; exposing tokens via normal tenant docs leaks credentials to all tenant members.
- `2026-03-05 | Finance V2 | Closed periods block posting` - Posting into a `finance_periods/{YYYY-MM}` document with `status=closed` must fail in functions and never be bypassed by client writes, otherwise monthly close reproducibility breaks.
- `2026-03-06 | Workspace Storage | Fallback only covers unconfigured providers` - Automatic fallback to Firebase is applied only when S3/Google Drive is not connected/configured; runtime provider errors are surfaced to the user and should be retried after fixing provider health.
- `2026-03-06 | Finance V2 AI Extraction | Responses API model options differ` - Some OpenAI models reject `temperature`; keep extraction calls model-compatible and avoid unsupported parameters to prevent 400 errors in production.
- `2026-03-06 | Finance V2 Confirm Draft | Undefined optional fields break Firestore writes` - Never pass `undefined` for optional fields like `projectId` into `addDoc`; strip undefined values before persisting transaction/bill payloads.
- `2026-03-06 | Finance V2 HTTP Access | Browser CORS fails on raw HTTP callable usage` - Prefer Firebase callable SDK invocation for finance mutation endpoints; direct browser fetch to Cloud Function URLs can fail preflight without explicit CORS handling.
- `2026-03-06 | Finance V2 Tests | React act warnings remain as follow-up (task w5Jz5gRPDxk5A34uq1Id)` - The suite can pass while still emitting `act(...)` warnings in FinanceTracking tests; treat as technical debt and resolve to reduce flaky async behavior.
- `2026-03-06 | Finance Functions Workspace | Period keys must stay YYYY-MM across wizard + payload` - Mixing freeform payload dates with invalid period formats breaks preview validation; keep canonical `YYYY-MM` values in both scope fields and advanced payload overrides.
- `2026-04-04 | Initiatives | Legacy milestone links may still point at idea ids` - Initiative rollout must resolve both first-class initiative ids and old `linkedInitiativeId` idea references until migration has rewritten existing milestone records.
- `2026-04-04 | ProjectFlow Deployment | Live initiative endpoints require deploy (task 07WnG8iY5oyM4KEEFkBi)` - Local Initiative V2 code adds initiative CRUD/upsert routes, but the hosted ProjectFlow API returns HTTP 404 for them until functions, rules, and any required indexes are deployed.
- `2026-04-05 | Initiative Feedback | Public screenshot uploads use base64 JSON` - The public initiative feedback endpoint accepts image attachments as data URLs instead of multipart uploads, so keep attachment count small and enforce strict size limits to avoid HTTP payload failures.
- `2026-04-05 | Initiative Feedback | Dynamic field roles drive task creation` - Public feedback fields are schema-driven, but only mapped roles like `title`, `description`, `customerName`, `customerEmail`, `company`, and `sourceUrl` populate first-class task metadata; all other custom fields are persisted as general feedback responses and appended to the task description.
- `2026-05-25 | Media Storage | Firebase signed image URLs expire before durable records do` - Do not store short-lived V4 signed URLs as durable image fields; persist managed file ids and deploy the media callables after Firebase CLI reauth so Firebase-backed files return token URLs. ProjectFlow task creation for this follow-up failed with API 500/503.
- `2026-05-25 | Firebase Storage | Closed billing account blocks all images` - If Firebase Storage images return HTTP 402 with `billing account for the owning project is disabled in state closed`, fix billing on `project-manager-9d0ad` first; rules, tokens, CORS, and React rendering cannot make objects readable while Google Storage rejects the bucket. The local service account can inspect Storage/Firestore but cannot deploy functions because it lacks `iam.serviceAccounts.ActAs` on `project-manager-9d0ad@appspot.gserviceaccount.com`.
- `2026-05-25 | ProjectFlow Tracking | Task/initiative writes can fail while app work succeeds` - ProjectFlow task/initiative creation returned HTTP 500/503 during the dashboard command-center session, so document tracking gaps locally and retry the linked project update later.
- `2026-05-26 | ProjectFlow Tracking | Initiative sync still failing for retention work` - `projectflow_cli.py sync checkpoint --entity initiative --phase start` returned an HTTP 500 HTML response while implementing the retention/usability plan, so local docs and git status are the source of truth until the ProjectFlow API accepts updates again.
- `2026-05-26 | ProjectFlow Tracking | Follow-up implementation sync now returns 403` - The Project Brief follow-up pass attempted `projectflow_cli.py sync checkpoint --entity initiative --phase start --project-id ogZ8Pyz8pwEQtv8I64nu` and received `HTTP 403: Insufficient permissions`; do not block implementation on tracking writes until token permissions are repaired.
- `2026-05-25 | Web i18n | Lazy bundle state must be a t() dependency` - If a lazy locale bundle loads but its state is missing from the `t` memo dependency list, screens can render raw translation keys until another unrelated language update occurs.
- `2026-05-25 | Health Snapshots | Scheduled snapshots lag behind web scoring` - The web health algorithm now considers schedule, tasks, issues, milestones, sprints, initiatives, flows, and engagement, but `functions/src/scheduler.ts` still snapshots only tasks/issues; create a ProjectFlow follow-up once task creation recovers from HTTP 500/503.
- `2026-05-25 | Project Lifecycle | Do not set paused/canceled states directly` - Project resume catch-up depends on `pausedAt` and `pausedFromStatus`, and canceled projects depend on cancel metadata, so use the overview lifecycle actions instead of writing `status: "On Hold"` or `status: "Canceled"` from generic settings. Canceled projects should stay out of health snapshots, spotlight, global user tasks, calendar scheduling pools, and AI current-work context even though their own project detail pages remain readable.
- `2026-05-25 | Dashboard Shell Scroll | AppLayout can keep dashboard rect/scrollTop math unchanged inside the authenticated shell, so scroll-driven dashboard reveals need wheel/touch gesture fallback in addition to container scroll listeners.
- `2026-05-26 | iOS Extensions | Synchronized extension folders copy Info.plist by default` - Add `PBXFileSystemSynchronizedBuildFileExceptionSet` entries for extension `Info.plist` files or Xcode will process and copy the same plist, producing duplicate-output build failures.
- `2026-05-26 | iOS Share Extension | TRUEPREDICATE blocks App Store submission` - Use explicit `NSExtensionActivationRule` keys for text/web URLs instead of `TRUEPREDICATE` so the extension remains App Store-valid.
- `2026-05-26 | ProjectFlow API | Codex endpoints must return JSON on failure` - Keep `handleProjectflowApiRoute` wrapped so new automation endpoints return diagnosable JSON errors instead of Firebase's default HTML 500 page.
- `2026-05-26 | Codex Integration | Follow-ups are mirrored docs plus tasks` - Codex follow-up creation writes both a normal task and a `codex_followups` inbox doc; keep the same `externalKey` on both so retries update instead of duplicating.
- `2026-05-26 | Cloud Functions Deploy | Runtime Config must be empty before March 2026 (task fOa0q1dGYfexIdGCO25C)` - Source no longer uses `functions.config()`, but stale Cloud Runtime Config entries can still trigger deploy warnings; remove them with `gcloud beta runtime-config` and verify a targeted function deploy no longer prints the deploy-time warning.
- `2026-05-26 | Cloud Functions Deploy | Keep v1 imports explicit on firebase-functions 7 (task IumAQAnwI3iph8Ps2gnN)` - The repo still uses many first-gen `functions.region(...)` exports; after upgrading to `firebase-functions` 7, import those modules from `firebase-functions/v1` while keeping scheduled functions on `firebase-functions/v2`.
- `2026-05-26 | Cloud Functions Deploy | Prefer targeted deploys for config-only validation` - A full `firebase deploy --only functions` can take a long time and may stall after many create/update operations; use a focused function deploy when validating environment/config warning changes.
- `2026-05-26 | Cloud Functions Deploy | Batch large dependency rollouts` - Deploying 100+ functions in batches of about 15 avoids the quota retry pile-up seen during an all-functions deploy and still lets each batch finish with a normal `Deploy complete`.
- `2026-05-26 | Hosting Deploy | googleDriveStorageCallback must be deployed before hosting rewrites reference it (task qJdKkyiHALJYNwyMxz8w)` - The rewrite is valid after deploying `functions:googleDriveStorageCallback`; if this warning returns, verify the function exists in `firebase functions:list` before relying on the OAuth route.
- `2026-05-26 | Web Push | VAPID is build-time config (task u7rRtb9TrHuLWxHKgYhl)` - The web push code is wired and local `web/.env.local` has `VITE_FIREBASE_VAPID_KEY`; production hosting still needs the same build-time env value plus a redeploy before browser token registration can be verified there.
- `2026-05-26 | iOS Push | Release APNs must be checked on the signed app (task YYawDDhIJguHIFkKqrZD)` - APNs/Firebase credentials are user-reported configured, but a Release archive can still be development-signed; production push readiness requires a signed `.app` whose entitlements report `aps-environment=production` plus a real device/TestFlight push test.
- `2026-05-26 | Notification Diagnostics | Self-tests must bypass client self-notification suppression` - The normal web `createNotification` helper intentionally skips notifications to yourself, so delivery tests must use the `sendTestNotification` callable and then read `notificationDeliveryLogs`.
- `2026-05-26 | Firestore Project Lookup | Collection-group documentId needs a full relative path` - Do not query `collectionGroup('projects')` with `where(documentId(), '==', projectId)`; use tenant-scoped reads first and owner/member-scoped discovery as fallback.
- `2026-05-26 | Initiatives | Converted tasks can recreate deleted initiatives` - When detaching a task with `convertedIdeaId`/`originIdeaId`, set `initiativeMigrationDismissed` or the legacy migration pass can recreate the deleted initiative on the next subscription.
- `2026-05-26 | ProjectFlow Tracking | Initiative create can fail while task create succeeds` - `initiative create` returned `HTTP 403: Insufficient permissions` during the iPhone redesign, but a normal completed task write succeeded; fall back to task tracking instead of blocking implementation.
- `2026-05-26 | Codex Tracking | Repo-local plugin needs exported token` - `plugins/projectflow-codex/scripts/projectflow_session.py` only reads `PROJECTFLOW_API_TOKEN`; when it is missing, use the ProjectFlow skill CLI Keychain auth path instead (task LyIsXWy1nnVFN0ujLwcN).
- `2026-05-27 | Browser QA | In-app screenshots can timeout on Project Overview` - `Page.captureScreenshot` timed out on `/project/:id` after layout changes even though DOM checks and interactions worked; use DOM metrics/console logs and retry screenshots from a fresh tab before treating it as an app failure.
- `2026-05-27 | Startup template seeds | Keep seeded founding work as normal records` - Startup/company templates should create editable tasks, initiatives, and milestones with `source: "template"`, `templateId`, `templateTrack`, and `templateSeedId` metadata instead of hidden special records, otherwise normal views, health, and cleanup flows drift from what founders can actually edit.
- `2026-05-27 | Startup jurisdiction templates | Store sources as metadata, not advice` - Jurisdiction-aware startup seeds should store official source references and review dates on the startup profile/task metadata, but still label legal/finance/compliance work as advisor-reviewed planning work. Do not hard-code country-specific legal conclusions into hidden automation.
- `2026-05-27 | Project Creation | Status options need global support` - When adding a create-wizard project status, update `ProjectStatus`, project list ordering/labels, edit/detail/profile status controls, and health/resume logic or newly created projects can look missing or render incorrectly.
- `2026-05-28 | Project Overview | Compact masthead actions can wrap on mobile` - Keep the masthead action row non-wrapping below 520px with flexible text buttons and fixed icon buttons, otherwise the settings icon can fall onto its own row and make the header feel taller than intended.
- `2026-05-28 | Project Overview | Embedded command strips need a single-column action layout` - When the command strip is placed inside a narrower focus panel, force the strip itself to one column and put lifecycle buttons in a two-column action row; otherwise metrics and actions collide on desktop.
- `2026-05-28 | Create Wizard Dark Mode | Selected icon chips need primary-token inversion` - Pair `--color-primary` with `--color-primary-text` for selected card/icon states; `--color-text-main` plus `--color-text-ondark` can resolve to light-on-light in dark mode.
- `2026-05-28 | Create Wizard Startup Setup | Match module selection` - Startup workstream selection should use the same visual contract as module selection: two-column cards, left icon, compact title/description, right check indicator, primary selected state, and no metadata summary above the grid.
- `2026-05-28 | Firestore Project Create | Strip nested undefined values` - Firestore rejects `undefined` anywhere in nested create payloads, not just top-level fields; company-project startup profiles must use conditional spreads and the project admin service should recursively remove undefined values before writes.
- `2026-05-28 | Startup UI Extensions | Reuse existing surfaces first` - Company-project additions should start from existing neutral cards, rows, metadata labels, and secondary actions; special warning panels, cockpit containers, and loud pills make the feature feel bolted on.
- `2026-05-28 | Project Purpose | Do not split description and objective` - Project creation, overview, settings, cards, and health scoring should use one user-facing project purpose; keep `brief.objective` as an internal compatibility mirror only, not a separate form entry or repeated overview block.
- `2026-05-28 | Project Overview | Empty attention is layout absence` - If the attention queue has no actionable item, remove the entire next-action section instead of showing a positive empty-state card in the command area.
- `2026-05-28 | Web Root Providers | Keep provider shell at entry root` - Keep the shared provider stack in `AppProviders`, but mount it from `index.tsx` around `App`; `App` should stay router-only, and tests that render it directly must wrap `AppProviders` explicitly. Moving the root provider ownership into `App` can leave Vite/Fast Refresh with a stale tree that renders `useUIState` consumers before `UIProvider`.
- `2026-05-28 | Project Overview | Empty sections can hide create actions` - Do not rely on section-local create buttons for resources that may have zero records; expose core creation commands from a persistent overview action surface as well.
- `2026-05-28 | Date Pickers | Portal popovers out of clipped modals` - Task and initiative create modals use hidden overflow for rounded shells, so shared DateTime popovers must render through `document.body` with viewport positioning instead of absolute positioning inside the field.
- `2026-05-28 | Create Wizard Critical Path | Keep optional setup post-create` - Cover images, GitHub repo linking, and resource links already have project overview surfaces; putting them in the create wizard makes project creation brittle and can block the core Firestore project write on optional integrations.
- `2026-05-28 | Create Wizard Selection Cards | Share the card contract` - Project type, module, startup workstream, member, and visibility choices should use the same selection-card component so hover, active, disabled, and check states cannot drift independently.
- `2026-05-28 | Create Wizard Selection Cards | Active hover needs active colors` - Pseudo-class hover selectors can beat selected/active classes in dark mode, so active cards must explicitly keep active background and text tokens on hover.
- `2026-05-28 | Create Wizard Selection Cards | Do not truncate descriptions` - Selection cards depend on balanced copy plus equal-height grid rows; single-line ellipsis makes project types unreadable and visually uneven.
- `2026-05-29 | Company Project Overview | Start with action, not briefing prose` - Unternehmensprojekte should not render a text-heavy cockpit or briefing block on the detail page; keep the first company-specific surface to founder action, readiness, compact workstreams, context, and linked delivery projects.
- `2026-05-29 | Company Project Overview | Keep workstreams and founding brief focused` - Do not list every startup workstream inline on the overview. Use a compact summary plus modal/drawer task inspector, and keep the Gründungsbriefing in its own overview modal rather than the project settings modal.
- `2026-05-28 | Create Wizard Visibility | Group mode needs explicit group choice` - Choosing group visibility must not auto-select the first workspace group; keep group mode as its own state and require an explicit group selection.
- `2026-05-28 | Create Wizard Cadence | Selection checks should not consume grid columns` - Compact cadence options need icon and label as the main layout; put the selected check in an overlay position so labels do not collapse.
- `2026-05-28 | Topbar Search | Avoid missing responsive utility classes` - The local Tailwind-migration utilities do not define every Tailwind variant such as `sm:block`; shell-critical controls like global search should use explicit SCSS classes instead. ProjectFlow task: `geMW8ffLW41FwAjaLjAH`.
- `2026-05-28 | Topbar Search Modal | Portal fixed overlays out of blurred pills` - Ancestors with `backdrop-filter` can constrain fixed-position descendants, so viewport-centered search overlays must render through `document.body` instead of inside the topbar pill. ProjectFlow task: `Mml405fi1CmrRGIRkE0f`.
- `2026-05-28 | Topbar Search Modal | Remove duplicate input focus treatments` - Spotlight-style search should keep focus treatment on the modal shell/row only; explicitly neutralize native input outlines and box shadows so Tailwind forms or browser defaults do not draw a second ring. ProjectFlow task: `bC17t1qePeZtKY8w9Fha`.
- `2026-05-28 | App Shell Topbar | Keep overlay and fallback in sync` - The active shell combines `AppLayout` utility classes with `.topbar-shell` SCSS; if the topbar should float over content, make both the loaded header and Suspense fallback non-reserving overlays and avoid `overflow-hidden` on the main content region. ProjectFlow task: `w7K9OvAzsmotVFqfKH6Q`.
- `2026-05-28 | Pinned Project Menu | Portal menus need stable viewport listeners` - Do not attach resize listeners with anonymous callbacks in portal dropdowns; keep a stable positioning callback, clamp from the trigger edge, and remove the same listener on cleanup. ProjectFlow task: `lLkOgelZZwVJVXYGcyqu`.
- `2026-05-28 | Workspace Loading | Keep first paint separate from enrichment` - Dashboard and Projects List should render from project metadata before loading secondary health/task/activity enrichment; also cache `ensureTenantAndUser` so per-project reads do not repeat bootstrap writes. ProjectFlow task: `dBGebEMPqHIvwxWOgyuF`.
- `2026-05-28 | ProjectFlow Tracking | Initiative checkpoint can still 403 while task sync works` - `sync checkpoint --entity initiative --phase start` returned `HTTP 403: Insufficient permissions` for project triage work, while task sync created `2Yjoy5YkLT36UFjshnKu`; keep falling back to task checkpoints when initiative permissions are blocked.
- `2026-05-30 | Web SCSS | App entry uses web/styles/index.scss` - `web/index.tsx` imports `./styles/index.scss`, not `web/src/styles/main.scss`; new component SCSS must be `@use`d from `web/styles/index.scss` or styles will not ship.
- `2026-05-30 | Workstream Sticky Command | Subtract main scroll padding from sticky top` - `.app-main-scroll--standard` adds `calc(--topbar-overlay-clearance + gutter)` top padding; sticky `top` inside that scrollport stacks on top (~100px gap below topbar). Use `--workstream-command-sticky-top: calc(var(--topbar-sticky-inset) - var(--app-main-scroll-padding-top))`.
- `2026-05-31 | Media Library | Tenant fallback must prefer active workspace` - Using `auth.currentUser.uid` as Media Library tenant fallback causes `tenant.media.upload` permission failures for workspace members; resolve tenant as explicit prop -> cached active tenant -> user UID.
- `2026-05-31 | Swift i18n | Avoid duplicate L10n.tr overloads` - A variadic `tr(_:args:fallback:)` overload that also labels the last parameter `fallback` makes `L10n.tr("key", fallback: "…")` recurse infinitely (stack overflow on Projects tab). Keep a single `tr(_:fallback:)` helper and use `String(format:)` at call sites when needed.
- `2026-05-31 | Swift tenant bootstrap | iOS must auto-discover workspace` - Unlike web `ensureActiveTenantId`, iOS only read UserDefaults; empty cache left stores never started and Projects stuck on “Syncing…”. Bootstrap from memberships + collectionGroup projects before showing tabs.
