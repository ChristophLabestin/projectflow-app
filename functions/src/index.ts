import { testSMTPConnection } from './email';
import { newsletterSubscribe, newsletterUnsubscribe, api } from './newsletter';

export { testSMTPConnection, newsletterSubscribe, newsletterUnsubscribe, api };
